Update the Nomekop Bequest landing page to match the reference design's visual aesthetic: a dark, immersive gaming theme with vibrant orange fire effects, dramatic shadows, and cinematic Pokemon character displays. The goal is to create a high-impact, professional gaming interface with orange as the primary accent color against deep dark backgrounds.
Key Visual Elements to Implement:

Deep black/charcoal backgrounds with orange fire gradients
Large, prominent Pokemon character displays with dramatic lighting
Orange gradient buttons with glow effects
Cinematic hero section with layered Pokemon sprites
Dark glass-morphism cards with orange accents
Fire particle effects and orange glow overlays
Professional stat displays with orange highlights
Smooth animations and transitions

Update the following files:

index.html - Update structure for new visual layout
style.css - Complete visual overhaul with orange/dark theme
script.js - Add fire particle effects and animations


PART A: COLOR SYSTEM - ORANGE/DARK THEME
1. Update CSS Variables (style.css)
REPLACE all color variables with orange/dark theme:
css/* ========================================
   ORANGE/DARK THEME - COLOR SYSTEM
   ======================================== */

:root {
  /* Background Colors - Deep Darks */
  --bg-black: #000000;
  --bg-darkest: #0a0a0a;
  --bg-dark: #121212;
  --bg-dark-secondary: #1a1a1a;
  --bg-dark-tertiary: #242424;
  
  /* Orange Palette - Primary Accent */
  --orange-50: #fff7ed;
  --orange-100: #ffedd5;
  --orange-200: #fed7aa;
  --orange-300: #fdba74;
  --orange-400: #fb923c;
  --orange-500: #f97316;
  --orange-600: #ea580c;
  --orange-700: #c2410c;
  --orange-800: #9a3412;
  --orange-900: #7c2d12;
  
  /* Primary Orange Gradient */
  --gradient-orange: linear-gradient(135deg, #ff6b35 0%, #ff8c42 50%, #ffa552 100%);
  --gradient-orange-intense: linear-gradient(135deg, #ff4500 0%, #ff6b35 50%, #ff8c42 100%);
  --gradient-fire: linear-gradient(180deg, #ff4500 0%, #ff6b35 40%, #ff8c42 70%, #ffb870 100%);
  
  /* Red/Orange Mix */
  --red-orange-500: #ff4500;
  --red-orange-600: #ff3d00;
  
  /* Text Colors */
  --text-white: #ffffff;
  --text-gray-50: #fafafa;
  --text-gray-100: #f5f5f5;
  --text-gray-200: #e5e5e5;
  --text-gray-300: #d4d4d4;
  --text-gray-400: #a3a3a3;
  --text-gray-500: #737373;
  --text-gray-600: #525252;
  
  /* Glow Effects - Orange */
  --glow-orange-sm: 0 0 10px rgba(249, 115, 22, 0.3);
  --glow-orange-md: 0 0 20px rgba(249, 115, 22, 0.4);
  --glow-orange-lg: 0 0 40px rgba(249, 115, 22, 0.5);
  --glow-orange-xl: 0 0 60px rgba(249, 115, 22, 0.6);
  
  --glow-fire: 0 0 80px rgba(255, 69, 0, 0.6);
  
  /* Card Backgrounds */
  --card-bg-dark: rgba(18, 18, 18, 0.85);
  --card-bg-darker: rgba(10, 10, 10, 0.9);
  --card-border-orange: rgba(249, 115, 22, 0.3);
  --card-border-orange-hover: rgba(249, 115, 22, 0.6);
  
  /* Shadows - Dark with Orange Tint */
  --shadow-dark: 0 4px 20px rgba(0, 0, 0, 0.8);
  --shadow-orange: 0 8px 32px rgba(249, 115, 22, 0.3);
  --shadow-orange-intense: 0 12px 48px rgba(255, 69, 0, 0.4);
  
  /* Border Radius */
  --radius-sm: 8px;
  --radius-md: 12px;
  --radius-lg: 16px;
  --radius-xl: 24px;
  --radius-2xl: 32px;
  --radius-full: 9999px;
  
  /* Spacing */
  --spacing-xs: 8px;
  --spacing-sm: 16px;
  --spacing-md: 24px;
  --spacing-lg: 32px;
  --spacing-xl: 48px;
  --spacing-2xl: 64px;
  --spacing-3xl: 96px;
  --spacing-4xl: 128px;
  
  /* Typography */
  --font-primary: 'Orbitron', sans-serif;
  --font-secondary: 'Inter', sans-serif;
}

PART B: GLOBAL BACKGROUND - FIRE & DARKNESS
2. Update Body & Background Styles (style.css)
css/* ========================================
   GLOBAL STYLES - DARK WITH FIRE
   ======================================== */

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html {
  scroll-behavior: smooth;
}

body {
  font-family: var(--font-secondary);
  background: var(--bg-black);
  color: var(--text-white);
  line-height: 1.6;
  overflow-x: hidden;
  position: relative;
}

/* Dark Background with Orange Glow Accents */
body::before {
  content: '';
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: 
    radial-gradient(ellipse at 20% 30%, rgba(255, 69, 0, 0.12) 0%, transparent 50%),
    radial-gradient(ellipse at 80% 70%, rgba(249, 115, 22, 0.08) 0%, transparent 50%),
    radial-gradient(ellipse at 50% 100%, rgba(255, 107, 53, 0.06) 0%, transparent 60%);
  pointer-events: none;
  z-index: 0;
  animation: glowPulse 8s ease-in-out infinite;
}

@keyframes glowPulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.7; }
}

/* Fire Particle Container */
.fire-particles {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  overflow: hidden;
  pointer-events: none;
  z-index: 1;
}

.fire-particle {
  position: absolute;
  width: 3px;
  height: 3px;
  background: var(--orange-500);
  border-radius: 50%;
  box-shadow: var(--glow-orange-sm);
  opacity: 0;
  animation: riseUp 8s infinite ease-out;
}

@keyframes riseUp {
  0% {
    opacity: 0;
    transform: translateY(0) scale(1);
  }
  10% {
    opacity: 0.8;
  }
  50% {
    opacity: 0.6;
  }
  100% {
    opacity: 0;
    transform: translateY(-100vh) scale(0.3);
  }
}

/* Main Content */
main {
  position: relative;
  z-index: 2;
}

PART C: HERO SECTION - CINEMATIC ORANGE THEME
3. Update Hero Section HTML (index.html)
REPLACE hero section with cinematic layout:
html<section id="hero" class="hero-section">
  <!-- Fire Overlay -->
  <div class="hero-fire-overlay"></div>
  
  <!-- Large Pokemon Character Left -->
  <div class="hero-pokemon-left">
    <img src="assets/pokeSprites/lucario.png" alt="Lucario" class="pokemon-sprite-large">
    <div class="pokemon-fire-glow"></div>
  </div>
  
  <!-- Large Pokemon Character Right (Optional) -->
  <div class="hero-pokemon-right">
    <img src="assets/pokeSprites/charizard.png" alt="Charizard" class="pokemon-sprite-large">
    <div class="pokemon-fire-glow"></div>
  </div>
  
  <!-- Hero Content -->
  <div class="hero-content-container">
    <div class="hero-content">
      <!-- Small Badge -->
      <div class="hero-badge-small">
        <span class="fire-icon">🔥</span>
        <span>THE NEW ERA</span>
      </div>
      
      <!-- Main Title -->
      <h1 class="hero-title-large">
        <span class="title-line">A Complete Gaming</span>
        <span class="title-line">Metaverse of Digital</span>
        <span class="title-line orange-glow">Monsters Awaits</span>
      </h1>
      
      <!-- Subtitle -->
      <p class="hero-description">
        Dive into a revolutionary gaming experience where classic monster-catching 
        meets blockchain technology. Own, trade, and battle with creatures that are 
        truly yours in a thriving digital economy.
      </p>
      
      <!-- CTA Buttons -->
      <div class="hero-buttons">
        <button class="btn-orange-primary btn-lg" id="hero-signup-btn">
          <span>Get Started</span>
          <svg class="btn-arrow" width="20" height="20" viewBox="0 0 20 20" fill="currentColor">
            <path d="M10.293 3.293a1 1 0 011.414 0l6 6a1 1 0 010 1.414l-6 6a1 1 0 01-1.414-1.414L14.586 11H3a1 1 0 110-2h11.586l-4.293-4.293a1 1 0 010-1.414z"/>
          </svg>
        </button>
        <button class="btn-orange-secondary btn-lg" id="hero-login-btn">
          <span>Login</span>
        </button>
      </div>
      
      <!-- Mini Stats -->
      <div class="hero-mini-stats">
        <div class="mini-stat">
          <div class="mini-stat-value">$1.6M+</div>
          <div class="mini-stat-label">Trading Volume</div>
        </div>
        <div class="mini-stat-divider"></div>
        <div class="mini-stat">
          <div class="mini-stat-value">230K+</div>
          <div class="mini-stat-label">Active Players</div>
        </div>
        <div class="mini-stat-divider"></div>
        <div class="mini-stat">
          <div class="mini-stat-value">$50K+</div>
          <div class="mini-stat-label">NFT Collection</div>
        </div>
      </div>
    </div>
  </div>
</section>
4. Hero Section Styling (style.css)
css/* ========================================
   HERO SECTION - CINEMATIC ORANGE/DARK
   ======================================== */

.hero-section {
  position: relative;
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--bg-black);
  overflow: hidden;
  padding: 140px 20px 80px;
}

/* Fire Overlay */
.hero-fire-overlay {
  position: absolute;
  bottom: 0;
  left: 0;
  width: 100%;
  height: 60%;
  background: linear-gradient(
    to top,
    rgba(255, 69, 0, 0.15) 0%,
    rgba(249, 115, 22, 0.1) 30%,
    transparent 100%
  );
  pointer-events: none;
  z-index: 1;
}

/* Large Pokemon Characters */
.hero-pokemon-left,
.hero-pokemon-right {
  position: absolute;
  width: 500px;
  height: 500px;
  z-index: 2;
}

.hero-pokemon-left {
  bottom: 0;
  left: -50px;
  animation: floatFire 5s ease-in-out infinite;
}

.hero-pokemon-right {
  bottom: 0;
  right: -50px;
  animation: floatFire 5s ease-in-out infinite 0.5s;
}

.pokemon-sprite-large {
  width: 100%;
  height: 100%;
  object-fit: contain;
  filter: drop-shadow(0 20px 80px rgba(255, 69, 0, 0.6));
}

.pokemon-fire-glow {
  position: absolute;
  bottom: 0;
  left: 50%;
  transform: translateX(-50%);
  width: 80%;
  height: 40%;
  background: radial-gradient(ellipse at center, rgba(255, 69, 0, 0.4) 0%, transparent 70%);
  filter: blur(40px);
  animation: fireGlow 3s ease-in-out infinite;
}

@keyframes floatFire {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-30px); }
}

@keyframes fireGlow {
  0%, 100% { opacity: 0.6; }
  50% { opacity: 1; }
}

/* Hero Content */
.hero-content-container {
  position: relative;
  z-index: 3;
  max-width: 1400px;
  width: 100%;
  margin: 0 auto;
}

.hero-content {
  max-width: 700px;
  margin: 0 auto;
  text-align: center;
}

/* Small Badge */
.hero-badge-small {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  padding: 8px 20px;
  background: rgba(255, 69, 0, 0.15);
  border: 1px solid rgba(249, 115, 22, 0.4);
  border-radius: var(--radius-full);
  font-size: 0.85rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 1.5px;
  color: var(--orange-400);
  margin-bottom: var(--spacing-lg);
  box-shadow: var(--glow-orange-sm);
}

.fire-icon {
  font-size: 1.2rem;
  animation: flicker 2s ease-in-out infinite;
}

@keyframes flicker {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.7; }
}

/* Large Title */
.hero-title-large {
  font-size: 5rem;
  font-weight: 900;
  font-family: var(--font-primary);
  line-height: 1.1;
  margin-bottom: var(--spacing-md);
  letter-spacing: -2px;
}

.title-line {
  display: block;
  color: var(--text-white);
  text-shadow: 0 4px 20px rgba(0, 0, 0, 0.8);
}

.orange-glow {
  background: var(--gradient-orange-intense);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  filter: drop-shadow(0 0 40px rgba(255, 69, 0, 0.6));
  animation: textGlow 3s ease-in-out infinite;
}

@keyframes textGlow {
  0%, 100% { filter: drop-shadow(0 0 40px rgba(255, 69, 0, 0.6)); }
  50% { filter: drop-shadow(0 0 60px rgba(255, 69, 0, 0.9)); }
}

/* Hero Description */
.hero-description {
  font-size: 1.15rem;
  line-height: 1.8;
  color: var(--text-gray-300);
  margin-bottom: var(--spacing-xl);
  max-width: 600px;
  margin-left: auto;
  margin-right: auto;
}

/* Hero Buttons */
.hero-buttons {
  display: flex;
  gap: 16px;
  justify-content: center;
  margin-bottom: var(--spacing-2xl);
  flex-wrap: wrap;
}

/* Mini Stats */
.hero-mini-stats {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: var(--spacing-md);
  padding: var(--spacing-md);
  background: rgba(18, 18, 18, 0.6);
  backdrop-filter: blur(20px);
  border: 1px solid rgba(249, 115, 22, 0.2);
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow-dark);
}

.mini-stat {
  text-align: center;
}

.mini-stat-value {
  font-size: 1.5rem;
  font-weight: 800;
  font-family: var(--font-primary);
  background: var(--gradient-orange);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  margin-bottom: 4px;
}

.mini-stat-label {
  font-size: 0.75rem;
  color: var(--text-gray-400);
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.mini-stat-divider {
  width: 1px;
  height: 40px;
  background: rgba(249, 115, 22, 0.2);
}

/* Mobile Responsive */
@media (max-width: 1200px) {
  .hero-pokemon-left,
  .hero-pokemon-right {
    width: 400px;
    height: 400px;
  }
}

@media (max-width: 968px) {
  .hero-title-large {
    font-size: 3.5rem;
  }
  
  .hero-pokemon-left {
    left: -100px;
    opacity: 0.4;
  }
  
  .hero-pokemon-right {
    right: -100px;
    opacity: 0.4;
  }
}

@media (max-width: 768px) {
  .hero-section {
    padding: 120px 20px 60px;
  }
  
  .hero-title-large {
    font-size: 2.5rem;
  }
  
  .hero-description {
    font-size: 1rem;
  }
  
  .hero-buttons {
    flex-direction: column;
    width: 100%;
    max-width: 400px;
    margin-left: auto;
    margin-right: auto;
  }
  
  .hero-pokemon-left,
  .hero-pokemon-right {
    display: none;
  }
  
  .hero-mini-stats {
    flex-wrap: wrap;
  }
  
  .mini-stat-divider {
    display: none;
  }
}

PART D: ORANGE BUTTON SYSTEM
5. Create Orange Button Components (style.css)
css/* ========================================
   ORANGE BUTTON SYSTEM
   ======================================== */

/* Primary Orange Button */
.btn-orange-primary {
  position: relative;
  display: inline-flex;
  align-items: center;
  gap: 10px;
  padding: 14px 32px;
  background: var(--gradient-orange-intense);
  border: none;
  border-radius: var(--radius-full);
  color: var(--text-white);
  font-size: 1rem;
  font-weight: 700;
  font-family: var(--font-primary);
  text-transform: uppercase;
  letter-spacing: 1px;
  cursor: pointer;
  overflow: hidden;
  transition: all 0.3s ease;
  box-shadow: var(--glow-orange-md);
}

.btn-orange-primary::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.4), transparent);
  transition: left 0.6s ease;
}

.btn-orange-primary:hover {
  transform: translateY(-3px);
  box-shadow: var(--glow-orange-xl), var(--shadow-orange-intense);
}

.btn-orange-primary:hover::before {
  left: 100%;
}

.btn-orange-primary:active {
  transform: translateY(-1px);
}

/* Secondary Orange Button (Outline) */
.btn-orange-secondary {
  display: inline-flex;
  align-items: center;
  gap: 10px;
  padding: 14px 32px;
  background: transparent;
  border: 2px solid var(--orange-500);
  border-radius: var(--radius-full);
  color: var(--text-white);
  font-size: 1rem;
  font-weight: 700;
  font-family: var(--font-primary);
  text-transform: uppercase;
  letter-spacing: 1px;
  cursor: pointer;
  position: relative;
  overflow: hidden;
  transition: all 0.3s ease;
}

.btn-orange-secondary::before {
  content: '';
  position: absolute;
  top: 50%;
  left: 50%;
  width: 0;
  height: 0;
  background: var(--orange-500);
  border-radius: 50%;
  transform: translate(-50%, -50%);
  transition: width 0.5s ease, height 0.5s ease;
  z-index: -1;
}

.btn-orange-secondary:hover {
  border-color: var(--orange-400);
  box-shadow: var(--glow-orange-lg);
  transform: translateY(-3px);
}

.btn-orange-secondary:hover::before {
  width: 400%;
  height: 400%;
}

/* Large Button Size */
.btn-lg {
  height: 58px;
  padding: 0 40px;
  font-size: 1.1rem;
}

/* Button Arrow Icon */
.btn-arrow {
  transition: transform 0.3s ease;
}

.btn-orange-primary:hover .btn-arrow {
  transform: translateX(5px);
}

/* Small Button */
.btn-sm {
  height: 42px;
  padding: 0 24px;
  font-size: 0.9rem;
}

PART E: DARK GLASS CARDS WITH ORANGE ACCENTS
6. Create Card Components (style.css)
css/* ========================================
   DARK GLASS CARDS - ORANGE THEME
   ======================================== */

/* Base Dark Glass Card */
.dark-glass-card {
  background: var(--card-bg-dark);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border: 1px solid var(--card-border-orange);
  border-radius: var(--radius-lg);
  padding: var(--spacing-lg);
  position: relative;
  overflow: hidden;
  transition: all 0.4s ease;
  box-shadow: var(--shadow-dark);
}

.dark-glass-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: radial-gradient(circle at top right, rgba(249, 115, 22, 0.1) 0%, transparent 60%);
  opacity: 0;
  transition: opacity 0.4s ease;
  pointer-events: none;
}

.dark-glass-card:hover {
  border-color: var(--card-border-orange-hover);
  transform: translateY(-8px);
  box-shadow: var(--shadow-dark), var(--shadow-orange);
}

.dark-glass-card:hover::before {
  opacity: 1;
}

/* Orange Glow Card */
.orange-glow-card {
  position: relative;
  background: var(--bg-dark-secondary);
  border-radius: var(--radius-lg);
  padding: var(--spacing-lg);
  overflow: hidden;
  transition: all 0.4s ease;
}

.orange-glow-card::before {
  content: '';
  position: absolute;
  inset: -2px;
  background: var(--gradient-orange-intense);
  border-radius: var(--radius-lg);
  opacity: 0;
  transition: opacity 0.4s ease;
  z-index: -1;
  animation: borderRotate 3s linear infinite;
}

.orange-glow-card::after {
  content: '';
  position: absolute;
  inset: 0;
  background: var(--bg-dark-secondary);
  border-radius: var(--radius-lg);
  z-index: -1;
}

.orange-glow-card:hover::before {
  opacity: 1;
}

@keyframes borderRotate {
  0% { filter: hue-rotate(0deg); }
  100% { filter: hue-rotate(360deg); }
}

/* Stat Card - Orange Theme */
.stat-card-orange {
  background: linear-gradient(135deg, rgba(255, 69, 0, 0.1), rgba(249, 115, 22, 0.05));
  backdrop-filter: blur(20px);
  border: 1px solid rgba(249, 115, 22, 0.3);
  border-radius: var(--radius-xl);
  padding: var(--spacing-md);
  text-align: center;
  position: relative;
  overflow: hidden;
  transition: all 0.3s ease;
}

.stat-card-orange::before {
  content: '';
  position: absolute;
  top: -50%;
  left: -50%;
  width: 200%;
  height: 200%;
  background: radial-gradient(circle, rgba(255, 69, 0, 0.3) 0%, transparent 70%);
  opacity: 0;
  transition: opacity 0.4s ease;
  animation: rotateSlow 10s linear infinite;
}

.stat-card-orange:hover::before {
  opacity: 1;
}

@keyframes rotateSlow {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}

.stat-card-icon {
  font-size: 2.5rem;
  margin-bottom: var(--spacing-sm);
  filter: drop-shadow(var(--glow-orange-md));
}

.stat-card-number {
  font-size: 2.5rem;
  font-weight: 800;
  font-family: var(--font-primary);
  background: var(--gradient-orange);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  margin-bottom: var(--spacing-xs);
}

.stat-card-label {
  font-size: 0.95rem;
  color: var(--text-gray-400);
  text-transform: uppercase;
  letter-spacing: 1px;
  font-weight: 600;
}

PART F: SECTION STYLES - ORANGE THEME
7. Update All Sections with Orange Theme (style.css)
css/* ========================================
   SECTIONS - ORANGE/DARK THEME
   ======================================== */

/* Section Headers */
.section-header {
  text-align: center;
  margin-bottom: var(--spacing-2xl);
}

.section-title {
  font-size: 3.5rem;
  font-weight: 900;
  font-family: var(--font-primary);
  margin-bottom: var(--spacing-sm);
  line-height: 1.2;
  color: var(--text-white);
}

.section-title .title-accent {
  background: var(--gradient-orange);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.section-subtitle {
  font-size: 1.2rem;
  color: var(--text-gray-300);
  max-width: 600px;
  margin: 0 auto;
}

/* Features Section */
.features-section {
  padding: var(--spacing-3xl) 20px;
  background: var(--bg-darkest);
  position: relative;
}

.features-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: var(--spacing-lg);
  max-width: 1400px;
  margin: 0 auto;
}

.feature-card {
  /* Use dark-glass-card class */
}

.feature-icon {
  width: 80px;
  height: 80px;
  margin: 0 auto var(--spacing-md);
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
}

.feature-type-icon {
  width: 64px;
  height: 64px;
  object-fit: contain;
  filter: drop-shadow(var(--glow-orange-md));
  transition: transform 0.3s ease;
}

.feature-card:hover .feature-type-icon {
  transform: scale(1.2) rotate(-5deg);
}

.feature-title {
  font-size: 1.5rem;
  font-weight: 700;
  font-family: var(--font-primary);
  color: var(--text-white);
  margin-bottom: var(--spacing-sm);
  text-align: center;
}

.feature-description {
  font-size: 1rem;
  color: var(--text-gray-300);
  line-height: 1.7;
  text-align: center;
}

/* Showcase Section */
.showcase-section {
  padding: var(--spacing-3xl) 20px;
  background: var(--bg-black);
  position: relative;
}

.showcase-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: var(--spacing-xl);
  max-width: 1200px;
  margin: 0 auto;
}

.creature-card {
  background: transparent;
  border: none;
  padding: var(--spacing-md);
  text-align: center;
  cursor: pointer;
  transition: transform 0.3s ease;
}

.creature-card:hover {
  transform: translateY(-12px);
}

.creature-image-container {
  position: relative;
  width: 100%;
  aspect-ratio: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: var(--spacing-md);
}

.type-icon {
  width: 60%;
  height: 60%;
  object-fit: contain;
  filter: drop-shadow(var(--glow-orange-lg));
  transition: transform 0.3s ease, filter 0.3s ease;
}

.creature-card:hover .type-icon {
  transform: scale(1.2);
  filter: drop-shadow(0 8px 40px rgba(255, 69, 0, 0.8));
}

.creature-name {
  font-size: 1.5rem;
  font-family: var(--font-primary);
  color: var(--text-white);
  margin-bottom: var(--spacing-xs);
  font-weight: 700;
}

.creature-type {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  margin-top: var(--spacing-sm);
}

.type-badge {
  width: 28px;
  height: 28px;
  object-fit: contain;
  filter: drop-shadow(0 2px 8px rgba(249, 115, 22, 0.4));
}

.type-text {
  font-size: 0.9rem;
  color: var(--text-gray-400);
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

/* Mobile Responsive */
@media (max-width: 1024px) {
  .showcase-grid {
    grid-template-columns: repeat(2, 1fr);
  }
}

@media (max-width: 768px) {
  .showcase-grid {
    grid-template-columns: 1fr;
  }
  
  .section-title {
    font-size: 2.5rem;
  }
}

PART G: FIRE PARTICLE SYSTEM
8. Add Fire Particle JavaScript (script.js)
javascript// ========================================
// FIRE PARTICLE SYSTEM
// ========================================

function createFireParticles() {
  const container = document.createElement('div');
  container.className = 'fire-particles';
  document.body.appendChild(container);
  
  // Create 40 fire particles
  for (let i = 0; i < 40; i++) {
    const particle = document.createElement('div');
    particle.className = 'fire-particle';
    
    // Random horizontal position
    particle.style.left = Math.random() * 100 + '%';
    
    // Random animation delay
    particle.style.animationDelay = Math.random() * 8 + 's';
    
    // Random animation duration (6-10 seconds)
    particle.style.animationDuration = (6 + Math.random() * 4) + 's';
    
    // Random size variation
    const size = 2 + Math.random() * 3;
    particle.style.width = size + 'px';
    particle.style.height = size + 'px';
    
    container.appendChild(particle);
  }
}

// Initialize fire particles on page load
document.addEventListener('DOMContentLoaded', () => {
  createFireParticles();
});

PART H: NAVIGATION - ORANGE THEME
9. Update Navigation Styling (style.css)
css/* ========================================
   NAVIGATION - ORANGE THEME
   ======================================== */

#main-nav {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  z-index: 10000;
  padding: 20px 40px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  transition: all 0.3s ease;
  background: transparent;
}

#main-nav.scrolled {
  background: rgba(10, 10, 10, 0.95);
  backdrop-filter: blur(20px);
  border-bottom: 1px solid rgba(249, 115, 22, 0.2);
  padding: 15px 40px;
  box-shadow: 0 4px 20px rgba(0, 0, 0, 0.8), 0 0 40px rgba(255, 69, 0, 0.1);
}

.nav-brand {
  display: flex;
  align-items: center;
  gap: 12px;
}

.nav-logo-icon {
  width: 40px;
  height: 40px;
  filter: drop-shadow(var(--glow-orange-md));
  transition: transform 0.3s ease;
}

.nav-brand:hover .nav-logo-icon {
  transform: rotate(360deg) scale(1.1);
  filter: drop-shadow(var(--glow-orange-lg));
}

.nav-brand-text {
  font-size: 1.3rem;
  font-weight: 800;
  font-family: var(--font-primary);
  background: var(--gradient-orange);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  letter-spacing: 1px;
}

/* Nav Login Button */
.btn-nav-login {
  height: 42px;
  padding: 0 28px;
  background: var(--gradient-orange-intense);
  border: none;
  border-radius: var(--radius-full);
  color: var(--text-white);
  font-size: 0.95rem;
  font-weight: 700;
  font-family: var(--font-primary);
  cursor: pointer;
  transition: all 0.3s ease;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  box-shadow: var(--glow-orange-sm);
}

.btn-nav-login:hover {
  transform: translateY(-2px);
  box-shadow: var(--glow-orange-md);
}

PART I: RESPONSIVE UPDATES
10. Mobile Optimizations (style.css)
css/* ========================================
   RESPONSIVE - MOBILE OPTIMIZATIONS
   ======================================== */

@media (max-width: 768px) {
  :root {
    --spacing-3xl: 60px;
    --spacing-2xl: 48px;
    --spacing-xl: 32px;
  }
  
  .hero-title-large {
    font-size: 2.2rem;
    letter-spacing: -1px;
  }
  
  .section-title {
    font-size: 2rem;
  }
  
  .features-grid,
  .showcase-grid {
    grid-template-columns: 1fr;
    gap: var(--spacing-md);
  }
  
  #main-nav {
    padding: 15px 20px;
  }
  
  #main-nav.scrolled {
    padding: 12px 20px;
  }
  
  .nav-brand-text {
    font-size: 1.1rem;
  }
}

IMPLEMENTATION CHECKLIST
11. Complete Implementation Steps
Phase 1: Color System

✅ Update all CSS variables to orange/dark theme
✅ Replace gradient definitions
✅ Update glow effects to orange
✅ Change shadow colors

Phase 2: Backgrounds

✅ Set body to pure black
✅ Add orange radial gradients
✅ Create fire particle system
✅ Add fire overlay to hero

Phase 3: Hero Section

✅ Update hero HTML structure
✅ Add large Pokemon sprites
✅ Style with orange gradients
✅ Add fire glow effects
✅ Update buttons to orange theme

Phase 4: Components

✅ Create orange button system
✅ Build dark glass cards
✅ Add orange glow cards
✅ Style stat cards with orange

Phase 5: Sections

✅ Update all section headers
✅ Apply orange accents
✅ Add dark backgrounds
✅ Update card styles

Phase 6: Effects

✅ Add fire particles
✅ Create glow animations
✅ Add hover effects
✅ Implement transitions

Phase 7: Navigation

✅ Update nav styling
✅ Add orange logo glow
✅ Style nav buttons
✅ Add scroll effects

Phase 8: Polish

✅ Test all animations
✅ Verify responsive design
✅ Check all colors
✅ Optimize performance


EXPECTED OUTCOME
After implementing this prompt, your landing page will have:
✅ Pure black background with orange fire accents
✅ Vibrant orange gradients as primary accent color
✅ Large cinematic Pokemon displays with fire glow effects
✅ Orange gradient buttons with glow and shine effects
✅ Dark glass-morphism cards with orange borders
✅ Fire particle system creating atmospheric depth
✅ Dramatic shadows and glows in orange tones
✅ Professional gaming aesthetic matching reference image
✅ Smooth animations and transitions throughout
✅ Fully responsive design for all devices
✅ Cohesive orange/dark theme across entire page